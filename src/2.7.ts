{
  // generic constraints with keyof operator:

  interface Vehicles {
    bike: string;
    car: string;
    truck: string;
    bus: string;
    boat: string;
    plane: string;
    train: string;
  }

  //
}
